<!-- BIRA1101AScript.jsp
程式目的：BIRA11檢查是否有輸入值
程式代號：BIRA11
程式日期：0950529
程式作者：Avery Yeh
--------------------------------------------------------
修改作者　　修改日期　　　修改目的
--------------------------------------------------------
-->
<%@ page language="java" contentType="text/html;charset=Big5" %>
<script language="Javascript">
<!--
function doBodyOnLoad(document)
{
  //nothing
}

function checkValue(form)
{
/*
	var isValid = true;
	var sError = "";
	
 	if (isBlank(form.issueId.value))
 	{
 		isValid = false;
 		sError = "請選擇期別";
 		 alert(sError);
 		return isValid;
 	}
 	zif (isBlank(form.elements['date(repayDate)'].value))
 	{
 		isValid = false;
 		sError = "請輸入預定買回日";
 		 alert(sError);
 		return isValid;
 	}
 	
 	if (isBlank(getInputValue(form.elements['BigDecimal(accountRate)'])))
 	{
 		isValid = false;
 		sError = "請輸入試算利率";
 		alert(sError);
 		return isValid;
 	}
 	
 	if (!CheckValidDate(form.elements['date(repayDate)'].value)){
		form.elements['date(repayDate)'].value = "";
		return false;
	}
	
	
	var use_default=(arguments.length>1)?arguments[1]:false;
	for(var i=0;i<form.elements['BigDecimal(accountRate)'].length;i++){
		if(isNaN(getSingleInputValue(form.elements['BigDecimal(accountRate)'][i],use_default))){
			isValid = false;
			setSingleInputValue(form.elements['BigDecimal(accountRate)'][i],"");
 			sError = "必須輸入數字";
 			alert(sError);
 			return isValid;
		}
	}
	/*
	if (isNaN(getInputText(form.elements['BigDecimal(accountRate)'])))
 	{
 		isValid = false;
 		sError = "必須輸入數字";
 		form.elements['BigDecimal(accountRate)'].value="";
 		form.elements['BigDecimal(accountRate)'].focus();
 		alert(sError);
 		return isValid;
 	}
 */
 /*
	if (isNaN(form.elements['BigDecimal(departRate)'].value))
 	{
 		isValid = false;
 		sError = "必須輸入數字";
 		form.elements['BigDecimal(departRate)'].value="";
 		form.elements['BigDecimal(departRate)'].focus();
 		alert(sError);
 		return isValid;
 	}
	return isValid;
	*/
}
//-->
</script>