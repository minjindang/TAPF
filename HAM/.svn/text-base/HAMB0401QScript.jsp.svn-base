<%@ page language="java" contentType="text/html;charset=Big5" %>
<script language="Javascript">
<!--
	function doBodyOnLoad(document)
	{
	  //nothing
	}

	function getSelectValue(obj)
	{
		if( obj.value!="" )
			return obj.options[obj.selectedIndex].text;
		else
			return "";
	}

	function checkForm(form)
	{
		var cols = new Array("qryAllYear","qryAllMonth");
		var colNames = new Array("記帳年度", "分配年月");
		var result = checkFormEmpty(form, cols);
		if(result >= 0){
			alert("【" + colNames[result] + "】，未輸入!!");
		}
		return result < 0;
	}
	function checkFill(form)
	{
		var cols = new Array("qryAllYear","qryAllMonth","faillAmount");
		var colNames = new Array("記帳年度", "分配年月","應撥補金額");
		var result = checkFormEmpty(form, cols);
		if(result >= 0){
			alert("【" + colNames[result] + "】，未輸入!!");
		}
		return result < 0;		
	}
//-->
</script>
