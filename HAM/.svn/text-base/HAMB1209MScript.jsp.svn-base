<%@ page language="java" contentType="text/html;charset=Big5" %>
<script language="Javascript">
<!--
function doBodyOnLoad(document)
{
  //nothing
}

function checkForm(form)
{
	var cols = new Array("qryYear");
	var colNames = new Array("記帳年度");
	var result = checkFormEmpty(form, cols);
	if(result >= 0){
		alert("【" + colNames[result] + "】，未輸入!!");
		form.focus(cols[result]);
	}
	return result < 0;
}

function checkForm2(form)
{
	var cols = new Array("qryYear","filePath");
	var colNames = new Array("記帳年度","檔案路徑");
	var result = checkFormEmpty(form, cols);
	if(result >= 0){
		alert("【" + colNames[result] + "】，未輸入!!");
		form.focus(cols[result]);
	}
	//if (result < 0){
		//return checkYear(form.qryYear.value);
	//}
	return result < 0;
}

//-->
</script>
