<!--	GARA1501QScript.jsp
程式目的：GARA1501
程式代號：GARA1501
程式日期：0950316
程式作者：ERIC CHEN
--------------------------------------------------------
修改作者　　修改日期　　　修改目的
--------------------------------------------------------
-->

<%@ page language="java" contentType="text/html;charset=Big5" %>
<script language="Javascript">
<!--
function doBodyOnLoad(document)
{
  //nothing
}
function getSelectValue(obj)
{
	if( obj.amountUnit_options.value!="" )
		obj.amountUnitLabel.value = obj.amountUnit_options[obj.amountUnit_options.selectedIndex].text;
	else
		obj.amountUnitLabel.value = "";
}
function checkValue(form)
{
	var isValid = true;
	var cols = new Array("issueSdate" );
	var colNames = new Array("【發行年度】");
	var result = checkFormEmpty(form, cols);
	if(result >= 0){
		alert(colNames[result] + "不可空白!!");
		isValid = false;
	}
	if (!CheckValidDate(form.elements['date(createDate)'].value)){
		form.elements['date(createDate)'].value = "";
		return false;
	}
	if ("0" == form.amountUnit.value)
    {
    	form.amountUnit.value = "1";
    }
    return isValid;
}

//-->
</script>
